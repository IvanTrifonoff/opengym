# Версионность openGym

Единый источник истины версии — **git-тег семвер `vX.Y.Z`**. Никакой второй
шкалы. `package.json` и `CHANGELOG.md` лишь зеркалят тег на момент релиза.

## Семвер

- **patch** `1.1.0 → 1.1.1` — фиксы, шлифовка UI, правки деплоя. Обратно-совместимо.
- **minor** `1.1 → 1.2.0` — новая фича (админка, лояльность, тренерский портал,
  пуши, удержание — каждая = +1 minor).
- **major** `1 → 2.0.0` — breaking change: смена домена/passkey, миграция БД,
  смена стека, несовместимый storage.

## Ветки

```
dev   — разработка, дев-площадка gym.trfnv.ru   (сборка+тесты на каждую фичу)
 │
 └──► main — прод, impulse.trfnv.ru              (ТОЛЬКО релизы)
```

Правило: **в `main` код попадает только через `scripts/release.sh`.** Нет
прямых пушей в `main`.

## Релизный цикл

1. Отфильтрованный скоуп доезжает в `dev`, тесты зелёные.
2. `bash scripts/release.sh [VERSION] [LABEL]`
3. Скрипт: синхронно бампит `frontend/package.json` + `api/package.json` + вставляет
   блок в `CHANGELOG.md`, ставит тег `vX.Y.Z`, вливает в `main` и пушит.
4. Деплой `main` на прод + смоук (`/api/health`, вход по passkey, PWA, пуши,
   `/admin`, `/trainer`, лояльность, удержание).

Красный build/тесты = релиза нет.

## Первичная настройка (уже сделана)

- Прод `impulse.trfnv.ru` стартует с `v1.0.0` (чистый старт, без истории users).
- Ветка `main` создастся первым же релизом из точки тега.
- Расхождение старой шкалы (CHANGELOG `1.10.x` vs `package.json` `1.2.x`)
  устранено — теперь одна шкала, отсчитываемая от `v1.0.0`.